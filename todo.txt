TODO
- シェル作る
- ファイル読み込んで実行
- メソッドの返り値が設定できない (return)
- ループを中断できない (break)
- 既存クラスのクラスメソッド対応表をどう定義するか
- トップレベルでのメソッド(関数)実装を認める？
- ルートオブジェクトをインタープリタオブジェクトとして使用すれば、バインディングは既にあるし、ルートでのメソッド定義も可能になる
- 数式リテラル
- プール辞書いる？
- 代入を「ｘｘｘ を、 ｙｙｙにする」とかにしたい。宣言されていない変数に何か特殊なオブジェクトを設定して、そのメソッドとして定義すればできる？
- ruby2exe とか使ってみる
- racc -E して racc も含めとかないと

DONE
- ブロックにwhileTrue、timesRepeatなんかを実装
- 真偽値を定義してif-then-elseを実装
- 負の値のリテラルがありません
- クラスメソッド(そもそも定義をどうするか)
- メソッドのバインディングで「親」をスーパークラスに設定 (微妙)
- メソッドのバインディングで「私」を自分自身に設定
- メソッド内で自分自身のメソッド呼び出し
- さらに、そして、そこで、加えて、また、ところで、などの接続詞は無視
- 複数行コメント
- 代入に「は」を使う
- インスタンス変数共有
